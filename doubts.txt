How do middlewares work in backend?
How does this error handling middleware work ?

How does this global error handling middleware work? I was only in previous versions of express, we had to use async wrappers and express-async-errors. Following is used in express 5+

app.use((err, req, res, next) => {
    console.error(err);
    res.status(500).json({ 
        success: false, 
        message: err.message || 'Something went wrong on the server' 
    });
})

Can you explain what node emitter is?
server.on("error", (error) => {
            console.log(error);
            process.exit(1);
        });